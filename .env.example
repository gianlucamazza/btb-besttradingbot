# Bybit API credentials for live trading
# Replace with your own credentials and rename this file to .env
BYBIT_API_KEY=your_api_key_here
BYBIT_API_SECRET=your_api_secret_here

# Optional: Debug logging (True/False)
# Set to True to enable debug logs, False for normal logging
DEBUG=False

# Note: The BYBIT_TESTNET setting is configured in the trading_config.yaml file
# under exchange.testnet rather than as an environment variable